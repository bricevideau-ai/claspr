[toolchain]
channel = "nightly-2026-05-22"
components = ["rust-src", "rustc-dev", "llvm-tools"]
# commit_hash = e96c36b6f76833388c519561d145492d2c08db4e

# Pinned to match `bricevideau-ai/rust-gpu` opencl-kernel-support. When
# bumping rust-gpu, sync this channel + commit hash with the rust-gpu
# rust-toolchain.toml.
