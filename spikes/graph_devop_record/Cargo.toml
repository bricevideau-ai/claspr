[package]
name = "claspr-graph-devop-record-spike"
version = "0.0.0"
edition = "2024"

# Standalone workspace (matches the other spikes). No claspr deps —
# the question is "what does the DeviceOperation trait need to grow
# to support recording into a cl_khr_command_buffer?", which is a
# trait-shape question, not a runtime question. Fake everything.
[workspace]

[dependencies]

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(target_arch, values("spirv"))'] }
