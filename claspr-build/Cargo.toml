[package]
name = "claspr-build"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Build-script helper for claspr — compile a rust-gpu kernel crate to SPIR-V at build time and emit a generated Rust module of typed launch wrappers"

[dependencies]
spirv-builder.workspace = true
syn = { version = "2", features = ["full", "extra-traits"] }
quote = "1"
proc-macro2 = "1"

[lints]
workspace = true
