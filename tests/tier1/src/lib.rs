//! Tier 1 integration test scaffolding. Tests live in `tests/*.rs`
//! (cargo's integration-test layout); this lib.rs is intentionally
//! empty so the crate compiles without exposing public surface.
