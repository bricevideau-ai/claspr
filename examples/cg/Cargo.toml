[package]
name = "cg-example"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Conjugate Gradient solver — a genuinely mixed CB-able/host-interpreted graph"

[[bin]]
name = "cg_example"
path = "src/main.rs"

[dependencies]
claspr = { path = "../../claspr" }

[build-dependencies]
claspr-build = { path = "../../claspr-build" }

[lints]
workspace = true
