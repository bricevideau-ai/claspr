[package]
name = "async-pipeline-example"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Multi-stage forward pass via claspr-async — upload → linear → activation → linear → download → host reduction"

[[bin]]
name = "async_pipeline_example"
path = "src/main.rs"

[dependencies]
claspr = { path = "../../claspr" }
claspr-async = { path = "../../claspr-async" }

[build-dependencies]
claspr-build = { path = "../../claspr-build" }

[lints]
workspace = true
