[package]
name = "image-pipeline"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Composes mandelbrot-kernel + sobel-kernel from a single host main — demonstrates claspr libraries as reusable crates"

[[bin]]
name = "image_pipeline"
path = "src/main.rs"

[dependencies]
claspr = { path = "../../claspr" }
mandelbrot-kernel = { path = "../mandelbrot-kernel" }
sobel-kernel = { path = "../sobel-kernel" }

[lints]
workspace = true
